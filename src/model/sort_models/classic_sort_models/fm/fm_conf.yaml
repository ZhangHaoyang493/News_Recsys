name: fm
l1_lambda: 1e-4
fm_embedding_dim: &dim 4

embeddings:

  embedding_tables:
    base_embedding_table:
      embedding_dims:
        user_id: *dim
        item_id: *dim
        category: *dim
        subcategory: *dim
        user_click_category: *dim
      share_emb_table_features: {}

    one_order_embedding_table:
      embedding_dims:
        user_id: 1
        item_id: 1
        category: 1
        subcategory: 1
        user_click_category: 1
      share_emb_table_features: {}